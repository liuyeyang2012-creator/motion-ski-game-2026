{
  "name": "motion-ski",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:hosting",
    "build:hosting": "tsc && vite build --base / && node scripts/stage-hosting-game.mjs && npm --prefix hosting-site ci --include=dev && npm --prefix hosting-site run build && node scripts/stage-hosting-dist.mjs",
    "build:pages": "tsc && vite build --mode pages --base /motion-ski-game-2026/ && node scripts/verify-pages-build.mjs",
    "preview": "vite preview",
    "test": "vitest",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "happy-dom": "^20.10.6",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
